docker pull ubuntu



docker run -it -p 8888:8888 ubuntu /bin/bash



cd /tmp

apt-get update

apt-get install -y curl vim

curl -O https://repo.anaconda.com/archive/Anaconda3-2019.10-Linux-x86_64.sh

bash Anaconda3-2019.10-Linux-x86_64.sh -b -p



cd ~

```shell
docker pull ubuntu
docker run -it -p 8888:8888 ubuntu /bin/bash

# 여기서는 도커 컨테이너 안에서
cd /tmp
apt-get update
apt-get install -y curl vim
curl -O https://repo.anaconda.com/archive/Anaconda3-2019.10-Linux-x86_64.sh
bash Anaconda3-2019.10-Linux-x86_64.sh -b -p
cd ~
echo 'PATH=$HOME/anaconda3/bin:$PATH' >> ~/.bashrc
source ~./bashrc
mkdir /root/workspace/jupyter
```







```shell
chmod 0755 start-jupyter-notebook.sh
./start-jupyter-notebook.sh
```



conda install pytorch



